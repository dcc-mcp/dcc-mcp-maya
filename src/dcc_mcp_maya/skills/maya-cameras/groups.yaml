groups:
- name: rendering
  description: Render settings, layers, passes, and output tools
  default_active: false
  tools:
  - create_camera
  - get_camera_info
  - list_all_cameras
  - set_active_camera
  - set_camera_attribute
